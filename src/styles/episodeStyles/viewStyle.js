import { StyleSheet } from "react-native"

export const viewStyle = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#1F2531',
    },  
});